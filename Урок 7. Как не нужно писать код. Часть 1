
// Задача 47: Задайте двумерный массив размером m×n,
// заполненный случайными вещественными числами.

int m = 3, n = 4;
double [,] mass = new double [m, n];

for (int i = 0; i < mass.GetLength(0); i++)
{
    for (int j = 0; j < mass.GetLength(1); j++)
    {
        mass[i, j] = Math.Round(new Random().Next(0, 10) + new Random().NextDouble(), 2);
        Console.Write(mass[i, j] + " ");
    }
    Console.WriteLine();
}




// Задача 50: Напишите программу, которая на вход
// принимает позиции элемента в двумерном массиве, и
// возвращает значение этого элемента или же указание,
// что такого элемента нет.

/*
Console.WriteLine ("Введите номер строки, в которой расположен искомый элемент");
int a = int.Parse(Console.ReadLine());
Console.WriteLine ("Введите номер столбца, в котором расположен искомый элемент");
int b = int.Parse(Console.ReadLine()); 

int m = 3, n = 4;

if ((a > m) || (b > n))
{
    Console.WriteLine("Такого элемента не существует в текущем массиве");
    return;
}

int [,] mass = new int [m, n];

for (int i = 0; i < mass.GetLength(0); i++)
{
    for (int j = 0; j < mass.GetLength(1); j++)
    {
        mass[i, j] = new Random().Next(1, 10);
        Console.Write(mass[i, j] + " ");
    }
    Console.WriteLine();
}
Console.WriteLine(mass[a, b]);
*/



// Задача 52: Задайте двумерный массив из целых чисел.
// Найдите среднее арифметическое элементов в каждом
// столбце.


/*
int [,] mass = new int [10, 10];

for (int i = 0; i < mass.GetLength(0); i++)
{
    for (int j = 0; j < mass.GetLength(1); j++)
    {
        mass[i, j] = new Random().Next(1, 10);
        Console.Write(mass[i, j] + " ");
    }
    Console.WriteLine();
}

int average = 0;

for (int i = 0; i < (mass.GetLength(1)); i++)
{
for (int j = 0; j < (mass.GetLength(0)); j++)
{
{
    average += mass[j, i];  
}
}
Console.Write(average / mass.GetLength(0) + ", ");
average = 0;
}

*/
